require 'test_helper'

class UserStoriesTest < ActionDispatch::IntegrationTest
  # User get's admin page. User tries login. User logs. User watch trap list.
  # User watch requests on chosen trap. User watch specific request information. User delete
  # specific request. User update trap. User delete trap. User watch users list
  # User watch specific user information. User updates specific user record. User delete
  # specific user record. User logout.

  #get '/admin'
  #assert_response :success
  #assert_template :index

end
